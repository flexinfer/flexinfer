apiVersion: v1
kind: Service
metadata:
  labels:
    app: google-workspace
    component: mcp-server
  name: mcp-google-workspace
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: google-workspace
  type: ClusterIP
