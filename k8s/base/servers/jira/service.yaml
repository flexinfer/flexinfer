apiVersion: v1
kind: Service
metadata:
  labels:
    app: jira
    component: mcp-server
  name: mcp-jira
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: jira
  type: ClusterIP
