apiVersion: v1
kind: Service
metadata:
  labels:
    app: asus-router
    component: mcp-server
  name: mcp-asus-router
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: asus-router
  type: ClusterIP
