apiVersion: v1
kind: Service
metadata:
  labels:
    app: helm
    component: mcp-server
  name: mcp-helm
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: helm
  type: ClusterIP
