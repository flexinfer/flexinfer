apiVersion: v1
kind: Service
metadata:
  labels:
    app: redis
    component: mcp-server
  name: mcp-redis
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: redis
  type: ClusterIP
