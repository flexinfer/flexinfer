apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gitlab
    component: mcp-server
    mcp.server/name: gitlab
    mcp.server/type: custom
  name: gitlab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitlab
  template:
    metadata:
      annotations:
        mcp.server/categories: devops,scm
        mcp.server/description: GitLab repository operations and management (Go native)
      labels:
        app: gitlab
        component: mcp-server
        mcp.server/name: gitlab
    spec:
      nodeSelector:
        kubernetes.io/arch: amd64
      containers:
        - env:
            - name: GITLAB_API_URL
              value: https://gitlab.flexinfer.ai/api/v4
            - name: GITLAB_PERSONAL_ACCESS_TOKEN
              valueFrom:
                secretKeyRef:
                  key: GITLAB_PERSONAL_ACCESS_TOKEN
                  name: loom-secrets
            - name: MCP_TRANSPORT
              value: websocket
            - name: MCP_WS_PORT
              value: "8080"
            - name: LOG_LEVEL
              value: info
            - name: MCP_SERVER_NAME
              value: gitlab
            - name: MCP_SERVER_COMMAND
              value: /app/services/loom-core/bin/mcp-gitlab
          image: registry.harbor.lan/mcp/custom-server:latest
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          name: gitlab
          ports:
            - containerPort: 8080
              name: websocket
          readinessProbe:
            httpGet:
              path: /ready
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 50m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: false
      securityContext:
        fsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
