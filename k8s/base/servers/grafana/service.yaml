apiVersion: v1
kind: Service
metadata:
  labels:
    app: grafana
    component: mcp-server
  name: mcp-grafana
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: grafana
  type: ClusterIP
