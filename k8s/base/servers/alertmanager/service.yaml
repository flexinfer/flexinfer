apiVersion: v1
kind: Service
metadata:
  labels:
    app: alertmanager
    component: mcp-server
  name: mcp-alertmanager
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: alertmanager
  type: ClusterIP
