apiVersion: v1
kind: Service
metadata:
  labels:
    app: release
    component: mcp-server
  name: mcp-release
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: release
  type: ClusterIP
