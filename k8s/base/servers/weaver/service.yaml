apiVersion: v1
kind: Service
metadata:
  labels:
    app: weaver
    component: mcp-server
  name: mcp-weaver
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: weaver
  type: ClusterIP
