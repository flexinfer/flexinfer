apiVersion: v1
kind: Service
metadata:
  labels:
    app: cloudflare
    component: mcp-server
  name: mcp-cloudflare
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: cloudflare
  type: ClusterIP
