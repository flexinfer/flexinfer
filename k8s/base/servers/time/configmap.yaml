apiVersion: v1
data:
  server.yaml: |
    always_allow: []
    categories:
        - utilities
    description: Timezone conversion and time operations
    name: time
    timeout: 30
kind: ConfigMap
metadata:
  labels:
    app: time
    component: mcp-server
  name: time-config
