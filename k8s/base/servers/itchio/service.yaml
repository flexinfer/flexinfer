apiVersion: v1
kind: Service
metadata:
  labels:
    app: itchio
    component: mcp-server
  name: mcp-itchio
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: itchio
  type: ClusterIP
