apiVersion: v1
kind: Service
metadata:
  labels:
    app: morph-embeddings
    component: mcp-server
  name: mcp-morph-embeddings
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: morph-embeddings
  type: ClusterIP
