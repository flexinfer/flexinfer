apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres
    component: mcp-server
  name: mcp-postgres
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: postgres
  type: ClusterIP
