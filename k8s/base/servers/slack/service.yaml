apiVersion: v1
kind: Service
metadata:
  labels:
    app: slack
    component: mcp-server
  name: mcp-slack
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: slack
  type: ClusterIP
