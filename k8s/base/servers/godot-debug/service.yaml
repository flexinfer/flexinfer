apiVersion: v1
kind: Service
metadata:
  labels:
    app: godot-debug
    component: mcp-server
  name: mcp-godot-debug
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: godot-debug
  type: ClusterIP
