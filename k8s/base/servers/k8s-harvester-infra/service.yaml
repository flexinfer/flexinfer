apiVersion: v1
kind: Service
metadata:
  labels:
    app: k8s-harvester-infra
    component: mcp-server
  name: mcp-k8s-harvester-infra
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: k8s-harvester-infra
  type: ClusterIP
