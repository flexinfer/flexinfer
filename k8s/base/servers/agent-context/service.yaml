apiVersion: v1
kind: Service
metadata:
  labels:
    app: agent-context
    component: mcp-server
  name: mcp-agent-context
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: agent-context
  type: ClusterIP
