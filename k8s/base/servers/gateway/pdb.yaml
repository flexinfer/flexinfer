apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  labels:
    app: loom-gateway
    component: loom-gateway
  name: loom-gateway
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: loom-gateway
