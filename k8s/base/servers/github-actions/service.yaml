apiVersion: v1
kind: Service
metadata:
  labels:
    app: github-actions
    component: mcp-server
  name: mcp-github-actions
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: github-actions
  type: ClusterIP
