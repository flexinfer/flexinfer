apiVersion: v1
kind: Service
metadata:
  labels:
    app: git-worktree
    component: mcp-server
  name: mcp-git-worktree
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: git-worktree
  type: ClusterIP
