apiVersion: v1
kind: Service
metadata:
  labels:
    app: codebase-memory
    component: mcp-server
  name: mcp-codebase-memory
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: codebase-memory
  type: ClusterIP
