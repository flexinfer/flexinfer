apiVersion: v1
kind: Service
metadata:
  labels:
    app: orchestra
    component: mcp-server
  name: mcp-orchestra
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: orchestra
  type: ClusterIP
