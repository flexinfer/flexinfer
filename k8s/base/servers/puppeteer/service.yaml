apiVersion: v1
kind: Service
metadata:
  labels:
    app: puppeteer
    component: mcp-server
  name: mcp-puppeteer
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: puppeteer
  type: ClusterIP
