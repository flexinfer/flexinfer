apiVersion: v1
kind: Service
metadata:
  labels:
    app: docker
    component: mcp-server
  name: mcp-docker
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: docker
  type: ClusterIP
