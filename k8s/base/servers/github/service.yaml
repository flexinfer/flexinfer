apiVersion: v1
kind: Service
metadata:
  labels:
    app: github
    component: mcp-server
  name: mcp-github
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: github
  type: ClusterIP
