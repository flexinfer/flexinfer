apiVersion: v1
kind: Service
metadata:
  labels:
    app: longhorn-k3s
    component: mcp-server
  name: mcp-longhorn-k3s
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: longhorn-k3s
  type: ClusterIP
