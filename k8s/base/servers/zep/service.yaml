apiVersion: v1
kind: Service
metadata:
  labels:
    app: zep
    component: mcp-server
  name: mcp-zep
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: zep
  type: ClusterIP
