apiVersion: v1
kind: Service
metadata:
  labels:
    app: devbox
    component: mcp-server
  name: mcp-devbox
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: devbox
  type: ClusterIP
