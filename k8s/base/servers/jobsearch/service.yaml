apiVersion: v1
kind: Service
metadata:
  labels:
    app: jobsearch
    component: mcp-server
  name: mcp-jobsearch
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: jobsearch
  type: ClusterIP
