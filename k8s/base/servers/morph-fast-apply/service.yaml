apiVersion: v1
kind: Service
metadata:
  labels:
    app: morph-fast-apply
    component: mcp-server
  name: mcp-morph-fast-apply
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: morph-fast-apply
  type: ClusterIP
