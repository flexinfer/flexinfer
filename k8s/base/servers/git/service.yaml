apiVersion: v1
kind: Service
metadata:
  labels:
    app: git
    component: mcp-server
  name: mcp-git
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: git
  type: ClusterIP
