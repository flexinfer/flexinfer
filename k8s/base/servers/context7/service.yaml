apiVersion: v1
kind: Service
metadata:
  labels:
    app: context7
    component: mcp-server
  name: mcp-context7
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: context7
  type: ClusterIP
