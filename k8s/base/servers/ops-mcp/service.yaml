apiVersion: v1
kind: Service
metadata:
  labels:
    app: ops-mcp
    component: mcp-server
  name: mcp-ops-mcp
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: ops-mcp
  type: ClusterIP
