# Node.js dependencies
node_modules/

# Editor / OS noise
.DS_Store
*.log

# NOTE: dist/ is intentionally tracked (not gitignored) so the bundled
# spawn-driver.js can be embedded into the Go binary via go:embed.
# Regenerate via `npm run build` once a real bundling step exists.
