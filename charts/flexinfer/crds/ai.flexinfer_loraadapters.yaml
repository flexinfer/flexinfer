---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: loraadapters.ai.flexinfer
spec:
  group: ai.flexinfer
  names:
    kind: LoRAAdapter
    listKind: LoRAAdapterList
    plural: loraadapters
    shortNames:
    - lora
    singular: loraadapter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.modelRef
      name: Model
      type: string
    - jsonPath: .spec.adapterName
      name: Adapter
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.loadedReplicas
      name: Loaded
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          LoRAAdapter is the Schema for managing LoRA adapter lifecycle.
          It enables declarative hot-swapping of LoRA adapters on running models.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoRAAdapterSpec defines the desired state of a LoRA adapter.
            properties:
              adapterName:
                description: |-
                  AdapterName is the unique name used in API requests to select this adapter.
                  This is the value clients pass in the "model" field to use the adapter.
                type: string
              maxRank:
                description: MaxRank limits the LoRA rank for this adapter.
                type: integer
              modelRef:
                description: ModelRef is the name of the parent Model CR this adapter
                  attaches to.
                type: string
              preload:
                description: |-
                  Preload causes the adapter to be loaded immediately when the model starts,
                  rather than waiting for the first request.
                type: boolean
              source:
                description: Source specifies where to find the adapter weights.
                properties:
                  type:
                    description: Type is the source type for adapter weights.
                    enum:
                    - HuggingFace
                    - OCI
                    - LocalPath
                    type: string
                  uri:
                    description: |-
                      URI is the source identifier.
                      For HuggingFace: "org/adapter-name"
                      For OCI: "registry.example.com/adapters/lora:v1"
                      For LocalPath: "/models/adapters/my-lora"
                    type: string
                required:
                - type
                - uri
                type: object
            required:
            - adapterName
            - modelRef
            - source
            type: object
          status:
            description: LoRAAdapterStatus defines the observed state of a LoRA adapter.
            properties:
              conditions:
                description: Conditions represent the latest observations of the adapter's
                  state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              loadedReplicas:
                description: LoadedReplicas is the number of model replicas where
                  this adapter is loaded.
                format: int32
                type: integer
              message:
                description: Message is a human-friendly status message.
                type: string
              phase:
                description: Phase is the current adapter lifecycle phase.
                type: string
              totalReplicas:
                description: TotalReplicas is the total number of ready replicas for
                  the parent model.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
