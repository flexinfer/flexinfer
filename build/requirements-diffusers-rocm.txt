# Pinned Python dependencies for Diffusers ROCm image
# Aligned with Dockerfile.diffusers-cuda versions where possible
# Last verified: 2026-03-01 on rocm/pytorch:rocm6.2.3

fastapi==0.115.6
uvicorn[standard]==0.34.0
diffusers==0.36.0
transformers==4.48.0
accelerate==0.34.0
safetensors==0.4.5
sentencepiece==0.2.0
protobuf==5.29.3
pillow==11.1.0
pydantic==2.10.5
python-multipart==0.0.20
aiofiles==24.1.0

# bitsandbytes is installed separately with --no-deps to prevent CUDA torch contamination
# See Dockerfile for the install command

# Force numpy < 2 for ROCm compatibility (installed separately with --force-reinstall)
# numpy<2
