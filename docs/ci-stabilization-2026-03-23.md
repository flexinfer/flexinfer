## CI Stabilization Notes

- backport the current GitLab pipeline template so branch pipelines fetch sources consistently
- remove local workspace-only Go module replaces from `go.mod`
- align internal module pins with published revisions used on the main development branch
