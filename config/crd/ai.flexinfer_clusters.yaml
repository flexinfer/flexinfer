---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: clusters.ai.flexinfer
spec:
  group: ai.flexinfer
  names:
    kind: Cluster
    listKind: ClusterList
    plural: clusters
    shortNames:
    - ficl
    singular: cluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.apiEndpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: Cluster represents a remote Kubernetes cluster registered for
          federation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterSpec defines how to connect to and classify a remote
              cluster.
            properties:
              apiEndpoint:
                description: |-
                  APIEndpoint is the Kubernetes API server URL for the remote cluster.
                  Must be an https URL.
                pattern: ^https://
                type: string
              labels:
                additionalProperties:
                  type: string
                description: |-
                  Labels are additional cluster metadata used for placement/routing policies.
                  Example: region, gpu-vendor, environment.
                type: object
              probeInterval:
                default: 30s
                description: ProbeInterval controls how often cluster health is checked.
                type: string
              secretRef:
                description: SecretRef points to a kubeconfig secret in the same namespace.
                properties:
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
            required:
            - apiEndpoint
            - secretRef
            type: object
          status:
            description: ClusterStatus defines observed health/capacity for a registered
              cluster.
            properties:
              available:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Available is the currently available capacity.
                type: object
              capacity:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Capacity is the total discovered capacity (for example
                  GPU resources).
                type: object
              conditions:
                description: Conditions represent detailed status conditions.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastProbeTime:
                description: LastProbeTime is the timestamp of the last successful
                  probe attempt.
                format: date-time
                type: string
              message:
                description: Message carries a human-readable status summary.
                type: string
              models:
                description: Models is the aggregated list of remote models.
                items:
                  description: ClusterModelStatus is an aggregated model status from
                    the remote cluster.
                  properties:
                    name:
                      description: Name is the model resource name.
                      type: string
                    namespace:
                      description: Namespace is the model resource namespace in the
                        remote cluster.
                      type: string
                    phase:
                      description: Phase is the remote model lifecycle phase.
                      type: string
                  required:
                  - name
                  - namespace
                  type: object
                type: array
              phase:
                description: Phase is the high-level cluster state.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
