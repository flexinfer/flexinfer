apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flexinfer-system
resources:
  # MLC compiled model references (shared NFS PVC)
  - mlc-shared-caches.yaml

  # === Abliteration + GPTQ pipelines (Download → Abliterate → Quantize → Publish) ===
  - abliteration-gptq.yaml           # qwen35-27b-opus-distill-gptq (radeonvii)
  - qwen35-9b-gptq.yaml             # qwen35-9b-gptq (radeonvii)
  - omnicoder-9b-gptq.yaml          # omnicoder-9b-gptq (7900xtx)

  # === OCI pull caches (Harbor → local PVC → Model CRs) ===
  - qwen35-27b-opus-distill-oci.yaml # → cblevins-7900xtx
  - qwen35-9b-oci.yaml              # → cblevins-5930k
  - omnicoder-9b-oci.yaml           # → cblevins-5930k
